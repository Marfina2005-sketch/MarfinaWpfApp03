<Window x:Class="MarfinaWpfApp03.SaleEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarfinaWpfApp03"
        mc:Ignorable="d"
         Title="Редактирование продажи" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#e74c3c"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c0392b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{Binding WindowTitle}" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Продукт:" Width="100" Style="{StaticResource LabelStyle}"/>
            <ComboBox x:Name="ProductComboBox" 
                      Width="300"
                      Margin="5"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding ProductId}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Количество:" Width="100" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="QuantityTextBox" 
                     Width="300"
                     Style="{StaticResource TextBoxStyle}"
                     Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Дата продажи:" Width="100" Style="{StaticResource LabelStyle}"/>
            <DatePicker x:Name="SaleDatePicker" 
                        Width="300"
                        Margin="5"
                        SelectedDate="{Binding SaleDate}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Сумма:" Width="100" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="TotalAmountTextBox" 
                     Width="300"
                     Style="{StaticResource TextBoxStyle}"
                     IsReadOnly="True"
                     Background="#f0f0f0"
                     Text="{Binding TotalAmount, StringFormat=C2}"/>
        </StackPanel>

        <TextBlock Grid.Row="5" 
                   Text="* Сумма рассчитывается автоматически" 
                   FontSize="11" 
                   Foreground="Gray"
                   Margin="105,5,0,0"
                   VerticalAlignment="Top"/>

        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,0">
            <Button Content="Сохранить" 
                    Style="{StaticResource ButtonStyle}"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена" 
                    Style="{StaticResource ButtonStyle}"
                    Click="CancelButton_Click"/>
            <Button x:Name="DeleteButton" 
                    Content="Удалить" 
                    Style="{StaticResource DangerButtonStyle}"
                    Click="DeleteButton_Click"
                    Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
